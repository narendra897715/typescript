const addnum = (a: number, b: number = 1) => a + b;

addnum(1);

//here u can see even though we are not passign second argumnet it is not throwign any error that is advantage of default parameter 
// important point default parameter should be places at last in all parameters